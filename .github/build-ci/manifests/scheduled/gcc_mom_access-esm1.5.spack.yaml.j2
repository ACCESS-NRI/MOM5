spack:
  specs:
  - mom5@git.{{ ref }}=access-esm1.5
  packages:
    oasis3-mct:
      require:
      - '@{{ oasis3_mct_version }}'
    netcdf-c:
      require:
      - '@{{ netcdf_c_version }}'
    netcdf-fortran:
      require:
      - '@{{ netcdf_fortran_version }}'
    openmpi:
      require:
      - '@{{ openmpi_version }}'
    gcc:
      require:
      - '@{{ gcc_compiler_version }}'
    all:
      require:
      - '%access_gcc'
      - 'target=x86_64'
  concretizer:
    unify: false
  view: false
